{
  "name": "daily-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Daily — production cloud backend (Postgres + REST /v1 + Claude generation + scheduling). Implements BACKEND-SCHEMA-API.md.",
  "engines": {
    "node": ">=20"
  },
  "main": "src/index.mjs",
  "scripts": {
    "start": "node src/index.mjs",
    "dev": "node --watch src/index.mjs",
    "migrate": "node src/db/migrate.mjs",
    "seed": "node src/db/seed.mjs",
    "smoke": "node scripts/smoke.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.65.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.13.1",
    "pg-mem": "^3.0.5",
    "pino": "^9.5.0",
    "pino-http": "^10.3.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "pino-pretty": "^13.0.0"
  }
}
